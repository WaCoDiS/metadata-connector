spring:
  application:
    name: metadata-connector
  profiles:
    active: docker
  cloud:
    config:
      uri: http://localhost:8888
management:
  security:
    enabled: false
  endpoints:
    web:
      exposure:
        include: "*"